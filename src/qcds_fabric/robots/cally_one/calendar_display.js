/* Cally.One calendar/time display dimension — projection only; no QCDS startup. */
(() => {
  if (window.__callyCalendarDisplayDimension) return;
  window.__callyCalendarDisplayDimension = true;

  const PREF_KEY='cally.one.display.v2';
  const LEGACY_PREF_KEY='cally.one.display.v1';
  const localZone=(()=>{try{return Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';}catch(_){return'UTC';}})();
  const uiLocale=()=>{try{return window.__callyLocale?.()||((navigator.language||'sv').toLowerCase().startsWith('sv')?'sv':'en');}catch(_){return'sv';}};
  const uiSv=()=>uiLocale()==='sv';
  const DISPLAY_LANGUAGES=[{code:'sv',locale:'sv-SE',labels:{sv:'Svenska',en:'Swedish'}},{code:'en',locale:'en-GB',labels:{sv:'Engelska',en:'English'}}];
  const CALENDARS=[['gregory',{sv:'Gregoriansk',en:'Gregorian'}],['iso8601',{sv:'ISO 8601',en:'ISO 8601'}],['islamic',{sv:'Islamisk',en:'Islamic'}],['islamic-umalqura',{sv:'Islamisk · Umm al-Qura',en:'Islamic · Umm al-Qura'}],['chinese',{sv:'Kinesisk',en:'Chinese'}],['hebrew',{sv:'Hebreisk',en:'Hebrew'}],['persian',{sv:'Persisk',en:'Persian'}],['indian',{sv:'Indisk nationalkalender',en:'Indian national calendar'}],['buddhist',{sv:'Buddhistisk',en:'Buddhist'}],['japanese',{sv:'Japansk era',en:'Japanese era'}]];

  function readPrefs(){try{const raw=localStorage.getItem(PREF_KEY)||localStorage.getItem(LEGACY_PREF_KEY)||'{}',p=JSON.parse(raw);return{calendar:String(p.calendar||'gregory'),timeZone:String(p.timeZone||localZone),hourCycle:['auto','h23','h12'].includes(p.hourCycle)?p.hourCycle:'auto',displayLocale:['sv','en'].includes(p.displayLocale)?p.displayLocale:(uiLocale()==='en'?'en':'sv')};}catch(_){return{calendar:'gregory',timeZone:localZone,hourCycle:'auto',displayLocale:uiLocale()==='en'?'en':'sv'};}}
  let prefs=readPrefs();
  const formatLocale=()=>DISPLAY_LANGUAGES.find(x=>x.code===prefs.displayLocale)?.locale||'sv-SE';
  function stateKey(){try{return window.__callySpaceStorageKey?.()||'cally.one.state.v1';}catch(_){return'cally.one.state.v1';}}
  function projectionDimension(key,label,value_kind,values=[]){return{key,label:label.sv,labels:label,value_kind,preferred:true,rich_editor:false,system:true,origin:'system',status:'active',hidden:false,values};}
  function writeProjectionState(){try{const state=JSON.parse(localStorage.getItem(stateKey())||'{}');if(!Array.isArray(state.dimension_states))state.dimension_states=[];const defs=[projectionDimension('calendar_display_language',{sv:'Kalenderns visningsspråk',en:'Calendar display language'},'language-state',DISPLAY_LANGUAGES.map(x=>({code:x.code,labels:x.labels,locale:x.locale}))),projectionDimension('calendar_system',{sv:'Tideräkning',en:'Calendar system'},'calendar-system-state',CALENDARS.map(([code,labels])=>({code,labels}))),projectionDimension('time_zone',{sv:'Tidszon',en:'Time zone'},'time-zone-state'),projectionDimension('clock_format',{sv:'Klockformat',en:'Clock format'},'clock-format-state',[{code:'auto',labels:{sv:'Automatiskt',en:'Automatic'}},{code:'h23',labels:{sv:'24 timmar',en:'24 hour'}},{code:'h12',labels:{sv:'12 timmar',en:'12 hour'}}])];for(const def of defs){const i=state.dimension_states.findIndex(x=>x&&x.key===def.key);if(i>=0)state.dimension_states[i]={...def,...state.dimension_states[i],values:state.dimension_states[i].values||def.values};else state.dimension_states.push(def);}state.calendar_projection={displayLocale:prefs.displayLocale,calendar:prefs.calendar,timeZone:prefs.timeZone,hourCycle:prefs.hourCycle};state.state_model={...(state.state_model||{}),calendar_projection_is_state:true,calendar_display_language_is_independent:true,calendar_system_is_independent:true,time_zone_is_independent:true};localStorage.setItem(stateKey(),JSON.stringify(state));}catch(_){}}
  function writePrefs(next){prefs=next;try{localStorage.setItem(PREF_KEY,JSON.stringify(next));}catch(_){}writeProjectionState();}

  function flag(code){if(code==='sv')return `<svg class="callyLangFlag" viewBox="0 0 28 20" aria-hidden="true"><rect width="28" height="20" rx="2" fill="#1769aa"/><rect x="8" width="3" height="20" fill="#ffd447"/><rect y="8" width="28" height="3" fill="#ffd447"/></svg>`;return `<svg class="callyLangFlag" viewBox="0 0 28 20" aria-hidden="true"><rect width="28" height="20" rx="2" fill="#21468b"/><path d="M0 0l28 20M28 0L0 20" stroke="#fff" stroke-width="5"/><path d="M0 0l28 20M28 0L0 20" stroke="#cf142b" stroke-width="2"/><path d="M14 0v20M0 10h28" stroke="#fff" stroke-width="6"/><path d="M14 0v20M0 10h28" stroke="#cf142b" stroke-width="3"/></svg>`;}
  function parseIsoDate(v){const[y,m,d]=String(v||'').slice(0,10).split('-').map(Number);return y&&m&&d?new Date(y,m-1,d,12,0,0,0):null;}
  function isoWeekNumber(date){const d=new Date(Date.UTC(date.getFullYear(),date.getMonth(),date.getDate())),day=d.getUTCDay()||7;d.setUTCDate(d.getUTCDate()+4-day);const ys=new Date(Date.UTC(d.getUTCFullYear(),0,1));return Math.ceil((((d-ys)/86400000)+1)/7);}
  function safeFormat(date,options){try{return new Intl.DateTimeFormat(formatLocale(),{...options,calendar:prefs.calendar,timeZone:prefs.timeZone}).format(date);}catch(_){return new Intl.DateTimeFormat(formatLocale(),{...options,timeZone:prefs.timeZone}).format(date);}}
  function calendarDay(date){try{const p=new Intl.DateTimeFormat(formatLocale(),{calendar:prefs.calendar,timeZone:prefs.timeZone,day:'numeric'}).formatToParts(date);return p.find(x=>x.type==='day')?.value||safeFormat(date,{day:'numeric'});}catch(_){return String(date.getDate());}}
  function calendarName(){const labels=CALENDARS.find(([id])=>id===prefs.calendar)?.[1];return labels?.[prefs.displayLocale]||labels?.en||prefs.calendar;}
  function zoneShort(){try{const p=new Intl.DateTimeFormat(formatLocale(),{timeZone:prefs.timeZone,timeZoneName:'short'}).formatToParts(new Date());return p.find(x=>x.type==='timeZoneName')?.value||prefs.timeZone;}catch(_){return prefs.timeZone;}}
  function formatClock(date){const o={timeZone:prefs.timeZone,hour:'numeric',minute:'2-digit',timeZoneName:'short'};if(prefs.hourCycle==='h12')o.hour12=true;if(prefs.hourCycle==='h23')o.hour12=false;try{return new Intl.DateTimeFormat(formatLocale(),o).format(date);}catch(_){return date.toLocaleTimeString(formatLocale(),{hour:'2-digit',minute:'2-digit'});}}
  function formatWallHour(hour){if(prefs.hourCycle==='h12'){const suffix=hour<12?'AM':'PM',h=hour%12||12;return`${h}:00 ${suffix}`;}if(prefs.hourCycle==='h23')return`${String(hour).padStart(2,'0')}:00`;try{return new Intl.DateTimeFormat(formatLocale(),{hour:'numeric',minute:'2-digit'}).format(new Date(2000,0,1,hour,0));}catch(_){return`${String(hour).padStart(2,'0')}:00`;}}
  function zonedGregorianParts(date){try{const p=new Intl.DateTimeFormat('en-CA',{calendar:'gregory',timeZone:prefs.timeZone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(date),get=t=>p.find(x=>x.type===t)?.value;return{year:+get('year'),month:+get('month'),day:+get('day'),hour:+get('hour'),minute:+get('minute')};}catch(_){return{year:date.getFullYear(),month:date.getMonth()+1,day:date.getDate(),hour:date.getHours(),minute:date.getMinutes()};}}
  function zonedDateKey(date){const p=zonedGregorianParts(date);return`${p.year}-${String(p.month).padStart(2,'0')}-${String(p.day).padStart(2,'0')}`;}
  function loadEventState(){try{const s=JSON.parse(localStorage.getItem(stateKey())||'{}');return Array.isArray(s.events)?s.events:[];}catch(_){return[];}}
  function supportedZones(){try{if(typeof Intl.supportedValuesOf==='function')return Intl.supportedValuesOf('timeZone');}catch(_){}return[localZone,'UTC','Europe/Stockholm','Europe/London','America/New_York','America/Los_Angeles','Asia/Tokyo','Asia/Shanghai','Asia/Dubai'];}

  function ensureMenuButtons(){if(document.querySelector('[data-cally-system-menu]'))return;const label=uiSv()?'Kalender & tid':'Calendar & time';document.querySelectorAll('.callyWideNav,.callyMobileMenu').forEach(nav=>{if(nav.querySelector('[data-cally-display-settings]'))return;const b=document.createElement('button');b.type='button';b.className='callyDisplayMenuButton';b.dataset.callyDisplaySettings='1';b.textContent=label;nav.appendChild(b);});}
  function ensureSettings(){let o=document.querySelector('#callyCalendarSettings');if(o)return o;o=document.createElement('div');o.id='callyCalendarSettings';o.className='callyCalendarSettings';o.innerHTML=`<div class="callyCalendarSettingsSheet" role="dialog" aria-modal="true" aria-labelledby="callyCalendarSettingsTitle"><div class="callyCalendarSettingsHead"><div><div class="eyebrow">${uiSv()?'KALENDERPROJEKTION':'CALENDAR PROJECTION'}</div><h2 id="callyCalendarSettingsTitle">${uiSv()?'Kalendervisning':'Calendar display'}</h2><p>${uiSv()?'Visningsspråk, tideräkning, tidszon och klockformat är separata dimensioner. De ändrar inte den underliggande tiden.':'Display language, calendar system, time zone and clock format are separate dimensions. They do not change the underlying instant.'}</p></div><button type="button" class="callyCalendarSettingsClose">×</button></div><div class="callyCalendarSettingsForm"><div class="callyDisplayLanguageField"><span>${uiSv()?'Visningsspråk i kalendern':'Calendar display language'}</span><div class="callyDisplayLanguageChoices">${DISPLAY_LANGUAGES.map(x=>`<button type="button" data-calendar-display-locale="${x.code}">${flag(x.code)}<b>${x.labels[uiLocale()]}</b></button>`).join('')}</div><small>${uiSv()?'Oberoende av huvudmenyns språk.':'Independent of the main-menu language.'}</small></div><label>${uiSv()?'Tideräkning / kalendersystem':'Calendar system'}<select id="callyCalendarSystem">${CALENDARS.map(([id,labels])=>`<option value="${id}">${labels[uiLocale()]||labels.en}</option>`).join('')}</select></label><label>${uiSv()?'Tidszon':'Time zone'}<input id="callyTimeZone" list="callyTimeZones" autocomplete="off"><datalist id="callyTimeZones"></datalist></label><label>${uiSv()?'Klockformat':'Clock format'}<select id="callyHourCycle"><option value="auto">${uiSv()?'Automatiskt för visningsspråket':'Automatic for display language'}</option><option value="h23">24 h · 18:30</option><option value="h12">12 h · 6:30 PM</option></select></label><div class="callyCalendarSettingsHint">${uiSv()?'Exempel: svenska menyer + English kalendertext + kinesisk tideräkning + Asia/Shanghai. Veckonummer är ISO 8601.':'Example: Swedish menus + English calendar text + Chinese calendar system + Asia/Shanghai. Week numbers use ISO 8601.'}</div><button type="button" class="callyCalendarSettingsSave">${uiSv()?'Använd projektion':'Use projection'}</button></div></div>`;document.body.appendChild(o);const list=o.querySelector('#callyTimeZones');supportedZones().forEach(zone=>{const x=document.createElement('option');x.value=zone;list.appendChild(x);});o.addEventListener('click',e=>{if(e.target===o||e.target.closest('.callyCalendarSettingsClose'))o.classList.remove('open');const lang=e.target.closest('[data-calendar-display-locale]');if(lang){prefs={...prefs,displayLocale:lang.dataset.calendarDisplayLocale};qsa('[data-calendar-display-locale]',o).forEach(b=>b.classList.toggle('active',b.dataset.calendarDisplayLocale===prefs.displayLocale));}});o.querySelector('.callyCalendarSettingsSave').addEventListener('click',()=>{const calendar=o.querySelector('#callyCalendarSystem').value||'gregory',timeZone=o.querySelector('#callyTimeZone').value.trim()||localZone,hourCycle=o.querySelector('#callyHourCycle').value||'auto';try{new Intl.DateTimeFormat(formatLocale(),{calendar,timeZone}).format(new Date());}catch(_){return;}writePrefs({calendar,timeZone,hourCycle,displayLocale:prefs.displayLocale});o.classList.remove('open');window.render?.();setTimeout(decorate,0);window.dispatchEvent(new CustomEvent('cally-one-ui-refresh'));});return o;}
  function openSettings(){prefs=readPrefs();const o=ensureSettings();o.querySelector('#callyCalendarSystem').value=prefs.calendar;o.querySelector('#callyTimeZone').value=prefs.timeZone;o.querySelector('#callyHourCycle').value=prefs.hourCycle;qsa('[data-calendar-display-locale]',o).forEach(b=>b.classList.toggle('active',b.dataset.calendarDisplayLocale===prefs.displayLocale));o.classList.add('open');}
  window.__callyOpenCalendarDisplaySettings=openSettings;

  function decorateContext(){const title=document.querySelector('#dateTitle');if(!title)return;title.querySelector('.callyCalendarContext')?.remove();const badge=document.createElement('span');badge.className='callyCalendarContext';badge.textContent=`${prefs.displayLocale.toUpperCase()} · ${calendarName()} · ${zoneShort()} · ${prefs.hourCycle==='h12'?'AM/PM':prefs.hourCycle==='h23'?'24 h':(uiSv()?'lokalt format':'locale format')}`;title.appendChild(badge);}
  function addWeekNumbers(){const timeline=document.querySelector('.timeline');if(timeline){const first=timeline.querySelector('.dayCol[data-drop-date]'),head=timeline.querySelector('.timeHead');if(first&&head){head.querySelector('.callyWeekNumber')?.remove();const d=parseIsoDate(first.dataset.dropDate);if(d)head.insertAdjacentHTML('beforeend',`<span class="callyWeekNumber">v ${isoWeekNumber(d)}</span>`);}}const cells=[...document.querySelectorAll('.month .dayCell[data-drop-date]')];cells.forEach((cell,i)=>{cell.querySelector('.callyMonthWeek')?.remove();if(i%7!==0)return;const d=parseIsoDate(cell.dataset.dropDate);if(d)cell.insertAdjacentHTML('afterbegin',`<span class="callyMonthWeek">v${isoWeekNumber(d)}</span>`);});}
  function decorateCalendarLabels(){const nonGregorian=prefs.calendar!=='gregory'&&prefs.calendar!=='iso8601';const heads=[...document.querySelectorAll('.timeline .dayHead')],cols=[...document.querySelectorAll('.timeline .dayCol[data-drop-date]')];heads.forEach((head,i)=>{head.querySelector('.callyAltDate')?.remove();const d=parseIsoDate(cols[i]?.dataset.dropDate);if(!d)return;head.dataset.callyDisplayDate=safeFormat(d,{weekday:'short',day:'numeric',month:'short'});if(nonGregorian){const alt=document.createElement('span');alt.className='callyAltDate';alt.textContent=safeFormat(d,{day:'numeric',month:'short',year:'numeric'});head.appendChild(alt);}});document.querySelectorAll('.month .dayCell[data-drop-date]').forEach(cell=>{const b=cell.querySelector('.dayNum'),d=parseIsoDate(cell.dataset.dropDate);if(!b||!d)return;if(!b.dataset.callyGregorianLabel)b.dataset.callyGregorianLabel=b.textContent;b.textContent=nonGregorian?calendarDay(d):b.dataset.callyGregorianLabel;b.title=safeFormat(d,{weekday:'long',day:'numeric',month:'long',year:'numeric'});});}
  function decorateTimelineTimeZone(){document.querySelectorAll('.timeRail .hour').forEach((h,i)=>{h.textContent=formatWallHour(i+6);});const timeline=document.querySelector('.timeline');if(!timeline)return;const columns=new Map([...timeline.querySelectorAll('.dayCol[data-drop-date]')].map(col=>[col.dataset.dropDate,col])),events=new Map(loadEventState().map(e=>[String(e.event_id),e]));timeline.querySelectorAll('.event[data-event-id]').forEach(el=>{const ev=events.get(String(el.dataset.eventId));if(!ev?.start)return;const start=new Date(ev.start),end=new Date(ev.end||ev.start),p=zonedGregorianParts(start),key=zonedDateKey(start),target=columns.get(key);if(target&&el.parentElement!==target)target.appendChild(el);const top=((p.hour+p.minute/60)-6)*59.5,height=Math.max(34,Math.max(0,end-start)/3600000*59.5-3);el.style.top=`${top}px`;el.style.height=`${height}px`;el.style.display=top<0||top>952?'none':'';const small=el.querySelector('small');if(small){const rest=small.textContent.includes(' · ')?small.textContent.split(' · ').slice(1).join(' · '):'';small.textContent=`${formatClock(start)}${rest?' · '+rest:''}`;}});const now=timeline.querySelector('.nowline');if(now){const p=zonedGregorianParts(new Date());now.style.top=`${((p.hour+p.minute/60)-6)*59.5}px`;}}
  function decorate(){prefs=readPrefs();writeProjectionState();ensureMenuButtons();ensureSettings();decorateContext();addWeekNumbers();decorateCalendarLabels();decorateTimelineTimeZone();}
  document.addEventListener('click',e=>{if(e.target.closest?.('[data-cally-display-settings]')){e.preventDefault();openSettings();}});
  window.addEventListener('cally-one-ui-refresh',decorate);
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',decorate,{once:true});else decorate();
})();